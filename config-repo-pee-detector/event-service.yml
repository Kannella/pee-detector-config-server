server:
  port: 8080

spring:
  application:
    name: event-service

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS

event-service:
  save-events: true
  persist-database: postgresql
  db-schema: public
