server:
  port: 8080

spring:
  application:
    name: config-repo-pee-detector

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS

logging:
  level:
    root: INFO
